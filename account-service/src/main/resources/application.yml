server:
  port: ${PORT:8001}

spring:
  application:
    name: account-service
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/gdb_accounts_db}
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    validate-on-migrate: false

logging:
  level:
    root: INFO
    com.gdb.account: DEBUG
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"
  file:
    name: logs/account-service.log
    max-size: 10MB
    max-history: 30

# External Service URLs
external:
  aadhar-service:
    url: ${AADHAR_SERVICE_URL:http://localhost:8005}
  company-service:
    url: ${COMPANY_SERVICE_URL:http://localhost:8006}
  notification-service:
    url: ${NOTIFICATION_SERVICE_URL:http://localhost:8007}
  auth-service:
    # FIX: Corrected plural "AUTH-SERVICES" name mismatch to matching singular application identity
    url: ${AUTH_SERVICE_URL:http://auth-service}

# Internal service client URLs (used by AuthClient.java @Value)
app:
  services:
    auth-url: ${AUTH_SERVICE_URL:http://localhost:8004}
    users-url: ${USERS_SERVICE_URL:http://users-service:8003}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true

# Swagger/OpenAPI Configuration
springdoc:
  api-docs:
    path: /api/v1/api-docs
  swagger-ui:
    path: /api/v1/docs
    operations-sorter: alpha
    tags-sorter: alpha
  packages-to-scan: com.gdb.account.controller

resilience4j:
  retry:
    instances:
      aadharServiceRetry:
        max-attempts: 3
        wait-duration: 2s
        retry-exceptions:
          - org.springframework.web.client.ResourceAccessException
